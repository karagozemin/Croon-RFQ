fastapi>=0.110
uvicorn[standard]>=0.29
sqlmodel>=0.0.16
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
python-dotenv>=1.0

# Live mode only (CROON_CAP_MODE=live). Not needed for the mock demo.
# Official CROO CAP Python SDK — imported lazily inside LiveCapClient, so
# mock mode works without it installed. Install explicitly when going live:
#   pip install croo-sdk
# croo-sdk>=0.1

